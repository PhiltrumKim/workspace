# 작업 요약 — Magewell RPA (2026-07-05)

하루 작업의 산출물 인덱스. 상세는 각 문서 참조.

## 1. 프로토콜/버그 수정
- **문서**: `수정내역_2026-07-05.md`
- 마우스 Δy 부호비트 정합성 수정(HW 검증: 펌웨어는 부호비트 미사용, 기능 무영향)
- GUI(`run_oo.py`) 수동 ROI 크래시 수정(cv2.selectROI → Tkinter 드래그)
- 추종 마우스 폭주/UI 프리즈 수정(프레임당 1패킷 제한 + 데드존, verbose off)
- 좌표 오프셋 자동 보정(~46px→~6px), 클릭 타깃을 검출박스 중심으로 변경

## 2. UI 커서 도달 테스트
- **보고서**: `ui_reach_report_20260705/UI도달테스트_보고서.md`
- **공유용 Artifact(HTML)**: `ui_reach_report_20260705/ui_reach_report.html`
  - 링크: https://claude.ai/code/artifact/bcf83728-cf07-4a31-ba88-aecbcabaeeb2
- 결과: 캡처 화면 15개 UI 요소 **15/15 커서 도달**(자동측정 12, 육안확인 3). 템플릿·결과 이미지 보관.

## 3. RPA 자율 제어 프레임워크 (핵심)
- **문서**: `RPA_자동제어_프레임워크.md`
- **코드**: `rpa_auto/`(engine, registry, workflow, discover)
- **데모**: `demo_autonomous.py`(발견+재실행), `demo_launch_browser.py`(브라우저 자동실행)
- **데이터**: `objects/`(클릭 객체), `workflows/`(워크플로우), `rpa_demo_20260705/`(증거 이미지)
- 결과: 아이콘=클릭객체 인식 → Menu 클릭으로 10개 항목 자동 발견/등록 + 워크플로우 생성 →
  재실행으로 Internet 서브메뉴 재현 → **`launch_web_browser` 워크플로우로 웹 브라우저 자동 실행 성공(사용자 개입 0)**.

## 다음에 이어서 할 후보
- OCR(pytesseract) 도입 → 항목을 의미 있는 이름으로 등록
- 재귀 발견으로 전체 UI 트리 구축, 목표 기반 워크플로우 자동 조립
- 키보드 자동화(URL 타이핑 등) 검증 — 현재 팝업 닫기는 마우스 `dismiss()` 사용
- 진단/실험용 임시 스크립트(`_*.py`, `test_*.py`, `menu_track_*.py` 등) 정리

## 현재 하드웨어 상태
- 대상 Pi 화면에 **웹 브라우저가 열린 상태**(자동실행 성공 상태)로 남아 있음. 릴레이/카메라 연결은 해제됨.
